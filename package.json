{
  "name": "datagraphx",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix nodejs-app/client run build",
    "dev": "npm run build && npm --prefix nodejs-app run dev",
    "start": "npm --prefix nodejs-app start"
  }
}
